form.js: Used to handle form interactions and validation.
navbar.js: Used to manage the main navigation bar, like toggling visibility or highlighting active links.
mobile-nav.js: Used to handle the mobile version of the navigation menu.
sticky-footer.js: Ensures the footer sticks to the bottom when content is not enough to fill the page.
footer.js: Manages interactions or dynamic features related to the footer.
lozad.min.js: Used for lazy loading images and elements to improve page load performance.
aos.js: Used for adding animations when elements scroll into view (scroll-triggered animations).
lenis.min.js: Used for smooth scrolling effects.
ukiyo.min.js: Used for creating parallax effects on images.
gsap.min.js: A powerful animation library for creating advanced animations.
ScrollMagic.min.js: Used for creating scroll-based animations, often in combination with GSAP.
animation.gsap.min.js: Integrates GSAP animations with ScrollMagic.
intlTelInput.min.js: Used for international telephone input fields with country code selection.
main.js: Usually contains the main custom JavaScript code for the website, combining various features and interactions.
